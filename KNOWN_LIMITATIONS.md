# 已知限制与未决项 (KNOWN_LIMITATIONS.md)

本文件按照实施规范，如实记录 v0.1 当前的技术边界、环境限制与后续待补齐部分。不得以“理论可行”掩盖未实际跑通的外部依赖。

## 1. 外部依赖与真机限制 (BLOCKED 项)
1. **中转站真实端点与密钥 (RELAY_AUTH / LIVE_TEST)**:
   - 当前工作区无真实中转站 API Key 与真实可用地址。
   - 所有中转站调用目前通过内置规范测试与占位诊断脚本验证，真实端点连通性测试标记为 **BLOCKED**。
2. **Android 编译链与真实真机 (ANDROID_SDK / HARDWARE)**:
   - 当前机器未安装或未配置 `ANDROID_HOME` / Android NDK。
   - Tauri 2 的 Android APK 生成 (`pnpm build:android`) 及真机键盘输入、物理返回键测试标记为 **NOT_RUN / BLOCKED**。已交付完整的移动端 React 代码、构建脚本及 Tauri Rust 移动端配置。

## 2. 业务功能边界 (符合 v0.1 需求设计)
1. **12306 查询非实时保证**:
   - 依赖上游 `JWJW000/12306-mcp` 爬取或接口响应，若 12306 触发高频风控、滑块验证码或格式变动，系统会如实向前端报告 `UPSTREAM_BLOCKED` 或 `MCP_SCHEMA_MISMATCH`，不会编造车票数据。
2. **严格单用户与单会话并发限制**:
   - 单个会话在同一时刻只允许一个活动 Run，避免上下文混乱。
3. **首版明确排除的功能**:
   - 不支持自动抢票、不接入支付网关、不支持短信验证码代填、不支持后台持续盯票推送。
