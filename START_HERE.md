# 编码智能体启动说明

本包是 Android 个人 AI 助手 v0.1 的实施规格，不是已实现的应用代码。

## 文件使用顺序

先读 `IMPLEMENTATION_PLAN.md`（唯一实施基线），再读 `server.env.example`。Word 文件是同内容的人工阅读版。`TEST_REPORT_TEMPLATE.md` 用于记录真实验收，不代表测试已执行。

## 可以直接发给编码智能体的任务指令

请依据本目录 `IMPLEMENTATION_PLAN.md` 开发 Android 个人 AI 助手 v0.1。

已确认：个人自用、Android、纯文字输入、iOS 原生感卡片；Tauri + React + Konsta UI + assistant-ui；TypeScript/Fastify 后端使用 Pi SDK，模型接我自己的中转站，后端部署到我自己的服务器，首个工具接 JWJW000/12306-mcp。

先检查当前工作区和未提交变更，不覆盖已有文件。先做 G0 的依赖与中转站工具链验证，再做契约、后端、UI、Android、安全和部署。不重新讨论技术栈，不加入语音、图片、定时查票、推送、自动购票、NAS、SSH、多 Agent 或插件市场。

API、事件、数据结构、幂等和状态行为以方案为准。生产禁用 Pi 内置通用文件/终端工具，禁止自动发现用户机器的扩展；MCP 使用固定提交和受控适配。工具 JSON 和 Error 文本必须识别，模型总结失败不能丢掉真实查询结果。

缺少中转站地址、密钥、服务器参数或签名文件时，使用占位配置继续完成不受阻塞部分。需要我提供凭据时列出配置项和安全注入方式，不要求把真实密钥写进仓库或聊天。真实调用、生产部署和破坏性操作分别授权。

每完成一关更新 docs/PROGRESS.md，列明改动、实际测试命令、结果和阻塞项。所有测试区分 fixture/live，未执行标 NOT_RUN，缺环境标 BLOCKED。不得用“理论可行”代替测试证据。完成后交付可运行工程、锁文件、部署文档、验收报告和在具备条件时构建的 APK。

现在从工作区检查、G0 验证计划和最小后端验证程序开始，而不是一次性生成整个应用。

## 第一轮应该看到的交付

1. 工作区与依赖核验记录；明确实际 Pi 导出、Node/Android 工具链版本。
2. 最小 Pi + 自定义无副作用工具程序；中转站配置样例和分阶段诊断。
3. 固定版 12306 MCP 连接、JSON 解析、错误文本识别测试。
4. docs/PROGRESS.md 与 COMPATIBILITY_REPORT.md；成功、失败、未测清楚分开。

## 提醒

方案中的 pnpm 脚本、API 和目录是需要你实现的契约，不是解压后已经存在的应用命令。本包没有真实 API Key、服务器登录信息、Android 签名密钥或实时车票。
